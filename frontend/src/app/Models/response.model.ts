export interface tokenInterface {
    token: string
  }

export interface productsResponse {
    _id: number,
    title: string,
    brand: string,
    price: number,
    thumbnail: string
}